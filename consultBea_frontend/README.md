# ConsultBea frontend

Minimal Vite React client for audio submissions.

```bash
npm install
npm run dev
```

Set `VITE_API_URL` to the backend base URL when it is not `http://localhost:3000`.

Set the backend `CORS_ORIGIN=http://localhost:5173` for local development, and set it to the matching deployed frontend origin in production.
